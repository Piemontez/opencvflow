import { ProjectTemplate } from '../../core/types/project-template';

const emptyProject = {
  group: 'Basic',
  title: 'Empty Project',
  action: () => {},
} as ProjectTemplate;

export default emptyProject;
